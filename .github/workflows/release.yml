name: Release

on:
  workflow_call:
    inputs:
      ref:
        description: "Git ref to build and release"
        required: true
        type: string
      tag:
        description: "Tag name for the release"
        required: true
        type: string
      pre-release:
        description: "Create as pre-release instead of draft"
        required: false
        type: boolean
        default: false

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    with:
      ref: ${{ inputs.ref }}

  release:
    name: Create release
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Clone tree
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4
      with:
        fetch-depth: 0
        ref: ${{ inputs.ref }}
    - name: Generate Changelog
      run: |
        if PREVIOUS_TAG=$(git describe --tags --match 'v*' --abbrev=0 HEAD^ 2>/dev/null); then
          git log --pretty=format:'* %h %s' --first-parent $PREVIOUS_TAG..HEAD > CHANGELOG.md
        fi
    - name: Download artifacts
      uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v4
      with:
        path: dist
        merge-multiple: true
    - name: Add transitionary package aliases
      run: |
        pushd dist
        cp xemu-${{ needs.build.outputs.pkg_version }}-windows-x86_64.zip xemu-win-x86_64-release.zip
        cp xemu-${{ needs.build.outputs.pkg_version }}-windows-arm64.zip xemu-win-aarch64-release.zip
    - name: Delete existing pre-release
      if: ${{ inputs.pre-release }}
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        if gh release view "${{ inputs.tag }}" --json isPrerelease --jq '.isPrerelease' | grep -q true; then
          gh release delete "${{ inputs.tag }}" --yes --cleanup-tag || true
        fi
    - name: Create release
      uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
      with:
        name: ${{ inputs.pre-release && format('Development Build (v{0})', needs.build.outputs.pkg_version) || inputs.tag }}
        tag_name: ${{ inputs.tag }}
        target_commitish: ${{ inputs.ref }}
        body_path: CHANGELOG.md
        draft: ${{ !inputs.pre-release }}
        prerelease: ${{ inputs.pre-release }}
        files: |
          dist/xemu*.tar.zst
          dist/xemu*-win*.zip
          dist/xemu*-macos-universal*.zip
          dist/xemu*.AppImage
