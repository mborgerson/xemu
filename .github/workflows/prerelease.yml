name: Pre-release

on:
  push:
    branches:
      - master

jobs:
  release:
    name: Pre-release
    uses: ./.github/workflows/release.yml
    with:
      ref: ${{ github.sha }}
      tag: pre-release
      pre-release: true
